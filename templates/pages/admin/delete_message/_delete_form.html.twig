<form method="post" action="{{ path('app_delete_message_delete', {'id': delete_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delete_message.id) }}">
    <button class="btn">Supprimer</button>
</form>
