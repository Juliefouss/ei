{% extends 'layout/admin.html.twig' %}


    {% block content %}
        <section class="page-header">
            <div class="container">
                <div class="row">
                    <div class="col-md-12">
                        <div class="content">
                            <h1 class="page-name"> Messages</h1>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="user-dashboard page-wrapper">
            <div class="container">
                <div class="row">
                    <div class="col-md-5">
                        <img class="img-responsive" src="{{ asset('images/PhotoMessage.jpg') }}" alt="photo message">
                    </div>
                    <div class="col-md-7">
                        <div class="dashboard-wrapper user-dashboard">
                            <div class="table-responsive">
                                <table class="table">
                                    <thead>
                                    <tr>
                                        <th>Nom</th>
                                        <th>email</th>
                                        <th>Object</th>
                                        <th></th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for contact_message in contact_messages %}
                                        <tr>
                                            <td>{{ contact_message.name }}</td>
                                            <td>{{ contact_message.firstname }}</td>
                                            <td>{{ contact_message.subject }}</td>
                                            <td>
                                                <a href="{{ path('app_contact_message_show', {'id': contact_message.id}) }}">
                                                    <button class="btn btn-transparent">Voir</button>
                                                </a>
{#                                                <form method="post"#}
{#                                                      action="{{ path('app_admin_message_delete', {'id': admin_message.id}) }}"#}
{#                                                      onsubmit="return confirm('Supprimer ce message?');">#}
{#                                                    <input type="hidden" name="_token"#}
{#                                                           value="{{ csrf_token('delete' ~ admin_message.id) }}">#}
{#                                                    <button class="btn btn-transparent">Supprimer</button>#}
{#                                                </form>#}
                                                {{ include('pages/admin/contact_message/_delete_form.html.twig') }}
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="6">no records found</td>
                                        </tr>
                                    {% endfor %}

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </section>
    {% endblock %}

{#    <table class="table">#}
{#        {% for contact_message in contact_messages %}#}
{#            <tr>#}
{#                <td>{{ contact_message.id }}</td>#}
{#                <td>{{ contact_message.name }}</td>#}
{#                <td>{{ contact_message.email }}</td>#}
{#                <td>{{ contact_message.subject }}</td>#}
{#                <td>{{ contact_message.message }}</td>#}
{#                <td>{{ contact_message.firstname }}</td>#}
{#                <td>#}
{#                    <a href="{{ path('app_contact_message_show', {'id': contact_message.id}) }}">show</a>#}
{#                    <a href="{{ path('app_contact_message_edit', {'id': contact_message.id}) }}">edit</a>#}
{#                </td>#}
{#            </tr>#}
{#        {% else %}#}
{#            <tr>#}
{#                <td colspan="7">no records found</td>#}
{#            </tr>#}
{#        {% endfor %}#}
{#    </table>#}

