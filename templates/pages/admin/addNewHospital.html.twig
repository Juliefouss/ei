{% extends 'layout/admin.html.twig' %}

{% block content %}
    <section class="signin-page account dark">
        <div class="container">
            <div class="row">
                <div class="col-md-6 col-md-offset-3">
                    <div class="block">

                        <h2 class="text-center">Ajouter un nouvel hôpital </h2>
                        {{ form(hospitalForm) }}
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}

