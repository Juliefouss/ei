{% extends 'layout/website.html.twig' %}

{% block content %}

    <section class=" page-404">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h2 class="title text-center ">Merci pour votre message</h2>

                    <a href ="{{ url('home') }}" class="btn btn-main"><i class="tf-ion-android-arrow-back"></i> Retour accueil</a>
                </div>
            </div>
        </div>
    </section>

{% endblock %}

