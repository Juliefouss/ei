{% extends 'layout/admin.html.twig' %}
{% block content %}
    <section class="page-wrapper ">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <img class="img-responsive" src="{{ asset('images/PhotoAdminUserHome.jpg') }}"
                         alt="photo accueil utilisateur">
                </div>
                <div class="col-md-6">
                    <div class="title text-center">
                        <h2> Bienvenue  {{ app.user.name }} {{ app.user.firstname }}</h2>
                    </div>
                    <div class="text-center">
                        <h5 class="widget-title padding">Merci de gérer les demandes dans cette ordre de priorité : </h5>
                    </div>
                   <div class="text-justify padding">
                       <p class="about">1. <button class="btn btn-transparent"><a href="{{ path('app_admin_message_index') }}">Messages médecins</a></button> </p>
                        <p class="about">2. <button class="btn btn-transparent"><a href="{{ path('app_hourly_index') }}">Gérer les annonces</a></button> </p>
                        <p class="about">3. <button class="btn btn-transparent"><a href="{{ path('app_contact_message_index') }}">Messages généraux</a></button> </p>
                    </div>

                    </div>
                </div>
            </div>
    </section>

{% endblock %}