{% extends 'layout/admin.html.twig' %}
{% block content %}
    <section class="page-wrapper ">
        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <img class="img-responsive" src="{{ asset('images/PhotoAdminUserHome.jpg') }}"
                         alt="photo accueil utilisateur">
                </div>
                <div class="col-md-6">
                    <div class="title text-center">
                        <h2> Bienvenue  {{ app.user.username }} {{ app.user.firstname }}</h2>
                    </div>

                    </div>
                </div>
            </div>
    </section>

{% endblock %}