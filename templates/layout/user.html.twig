{% extends 'layout/basic.html.twig' %}
{% block navigation %}

<!-- Main Menu Section -->
<section class="menu">
    <nav class="navbar navigation background-blue">
        <div class="container">
            <!-- Navbar Links -->
            <div id="navbar" class="navbar-collapse collapse text-center ">
                <ul class="nav navbar-nav">

                    <!-- Home -->
                    <li class="dropdown ">
                        <a href="{{ url('user-home') }}" >Home</a>
                    </li><!-- / Home -->

                    <!--Apply--->

                    <li class="dropdown ">
                        <a href="{{ url('app_hourly_user-index') }}">Annonces</a>
                    </li>
                    <!-- Message -->

                    <li class="dropdown ">
                        <a href="{{ url('app_user_message_index') }}">Messages</a>
                    </li>

                    <!-- Profil -->

                    <li class="dropdown ">
                        <a href="{{ url('app_user_profile_index'  , {'id': app.user.id}) }}">Mon profil</a>
                    </li>

                    <!-- lien vers admin -->

                    {% if is_granted("ROLE_ADMIN") %}
                    <li class="dropdown ">
                        <a href="{{ url('admin-home') }}"><i class="fa-solid fa-user-secret"></i></a>
                    </li>
                    {% endif %}

                    <!-- Deconnexion -->

                    <li class="dropdown ">
                        <a href="{{ url('app_logout') }}">Déconnexion</a>
                    </li>


                </ul><!-- / .nav .navbar-nav -->

            </div>
            <!--/.navbar-collapse -->
        </div><!-- / .container -->
    </nav>
</section>



{% endblock %}

{% block content %}
{% endblock %}